# AI-Based Smart Drainage Waste Detection - Phase 5
# Category-wise Waste Counting using YOLO tracking (persistent IDs)
#
# Install with:
#   pip install -r requirements.txt
#
# NOTE: This installs the CPU version of everything. No GPU is required.
# The 'ultralytics' package will automatically pull in a matching CPU
# build of torch if one isn't already installed. 'ultralytics' also
# ships the built-in tracker configs (bytetrack.yaml) used here.

ultralytics>=8.3.0
opencv-python>=4.9.0
numpy>=1.26.0
lap>=0.5.12
